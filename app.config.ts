export default defineAppConfig({
  ui: {
    primary: "blue",
    gray: "cool",
    // container: {
    // 	constrained: 'max-w-8xl',
    // },

    //make nuxt-icon(namely iconify) available
    icons: {
      dynamic: true,
    },
  },
});
